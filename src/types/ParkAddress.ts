export type ParkAddress = {
   pmaid: string;
   loc_id: string;
   name: string;
   address: string;
   zip_code: string;
   x_coord: string;
   y_coord: string;
   location_1: {
      latitude: string;
      longitude: string;
   };
}